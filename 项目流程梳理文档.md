# 🚗 汽车美容店管理系统 — 项目流程梳理文档

> **版本**: v1.0  
> **日期**: 2026-07-07  
> **作者**: AttentionCasria  

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [数据库设计](#3-数据库设计)
4. [功能模块与业务流程](#4-功能模块与业务流程)
5. [API 接口设计](#5-api-接口设计)
6. [前端页面结构](#6-前端页面结构)
7. [部署与运行](#7-部署与运行)

---

## 1. 项目概述

### 1.1 项目背景

汽车美容店管理系统是一款面向汽车美容服务门店的全栈单页管理应用。系统覆盖了从**美容项目管理 → 客户/车辆登记 → 订单创建 → 施工收款 → 统计分析 → 数据备份恢复**的完整业务闭环。

### 1.2 核心功能

| 序号 | 功能模块 | 说明 |
|------|---------|------|
| 1 | 美容项目管理 | 服务项目的增删改查、价格管理、启用/停用 |
| 2 | 客户信息管理 | 客户基本信息维护，手机号唯一性校验，性别约束 |
| 3 | 车辆管理 | 客户车辆信息管理，关联客户，车牌号唯一 |
| 4 | 美容登记与收费 | 订单创建（多项目）、状态流转、收款结算 |
| 5 | 统计分析 | 月度项目统计、年度客户统计、月度收入统计 |
| 6 | 数据备份恢复 | 数据库备份（mysqldump）、恢复、下载 |

### 1.3 项目结构总览

```
car-manage-system/
├── README.md                          # 项目说明
├── API接口文档.md                      # 详细 API 文档
├── 项目流程梳理文档.md                  # 本文档
├── backend/                           # 后端 — Spring Boot
│   ├── pom.xml                        # Maven 依赖
│   └── src/main/
│       ├── java/com/carmanage/
│       │   ├── CarManageApplication.java   # 启动类
│       │   ├── common/                     # Result、PageResult、异常处理
│       │   ├── config/                     # CORS、MyBatis-Plus、自动填充
│       │   ├── controller/                 # 6 个 Controller
│       │   ├── entity/                     # 7 个 Entity
│       │   ├── mapper/                     # 7 个 Mapper 接口
│       │   └── service/                    # 6 个 Service（接口 + 实现）
│       └── resources/
│           ├── application.yml             # 应用配置
│           ├── db/init.sql                 # 建库建表 + 测试数据
│           └── mapper/                     # 5 个 XML 映射文件
└── frontend/                          # 前端 — Vue 3
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── main.js                    # Vue 应用入口
        ├── App.vue                    # 根组件
        ├── api/                       # 6 个 API 模块 + Axios 封装
        ├── router/index.js            # 路由配置
        ├── components/Layout.vue      # 整体布局（侧边栏 + 顶栏）
        └── views/                     # 7 个页面视图
```

---

## 2. 技术架构

### 2.1 技术选型

| 层面 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **后端框架** | Spring Boot | 2.7.18 | RESTful API 服务 |
| **JDK** | Java | 1.8 | 运行环境 |
| **ORM** | MyBatis-Plus | 3.5.5 | 数据库操作、分页、逻辑删除 |
| **数据库** | MySQL | 8.0+ | 数据持久化 |
| **连接池** | Druid | 1.2.20 | 数据库连接池 |
| **JSON** | Fastjson | 2.0.43 | JSON 序列化/反序列化 |
| **工具** | Lombok | — | 减少样板代码 |
| **前端框架** | Vue | 3.4.15 | 响应式 UI 框架 |
| **构建工具** | Vite | 5.0.11 | 前端构建与热更新 |
| **UI 组件库** | Element Plus | 2.5.3 | 后台管理 UI 组件 |
| **路由** | Vue Router | 4.2.5 | 前端路由 |
| **HTTP 客户端** | Axios | 1.6.5 | API 请求 |
| **图标库** | @element-plus/icons-vue | 2.3.1 | 图标组件 |

### 2.2 架构图

```
┌──────────────────────────────────────────────────────┐
│                    浏览器 (Vue 3)                      │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────────┐ │
│  │Dashboard│ │ 项目管理 │ │ 客户管理 │ │ 订单管理 …  │ │
│  └────┬────┘ └────┬────┘ └────┬────┘ └─────┬──────┘ │
│       └───────────┴───────────┴─────────────┘         │
│                        │ Axios                        │
│                        │ /api 代理                     │
└────────────────────────┼──────────────────────────────┘
                         │ HTTP RESTful
┌────────────────────────┼──────────────────────────────┐
│                  Spring Boot (8080)                    │
│  ┌─────────────────────┼──────────────────────────┐   │
│  │              Controller 层 (6个)                 │   │
│  │  BeautyItemController, CustomerController,      │   │
│  │  VehicleController, BeautyOrderController,      │   │
│  │  StatisticsController, BackupController         │   │
│  └─────────────────────┼──────────────────────────┘   │
│  ┌─────────────────────┼──────────────────────────┐   │
│  │               Service 层 (6个)                   │   │
│  │  业务逻辑、事务管理、金额计算                        │   │
│  └─────────────────────┼──────────────────────────┘   │
│  ┌─────────────────────┼──────────────────────────┐   │
│  │           Mapper 层 (MyBatis-Plus)               │   │
│  │  基础 CRUD + 自定义 XML SQL + JdbcTemplate        │   │
│  └─────────────────────┼──────────────────────────┘   │
└────────────────────────┼──────────────────────────────┘
                         │ JDBC
┌────────────────────────┼──────────────────────────────┐
│                  MySQL (3306)                          │
│            数据库: car_beauty (utf8mb4)                 │
│  7 张表: customers, vehicles, beauty_items,           │
│          beauty_orders, order_items, payments,        │
│          backup_records                               │
└───────────────────────────────────────────────────────┘
```

---

## 3. 数据库设计

> 数据库名: `car_beauty`  
> 字符集: `utf8mb4`  
> 排序规则: `utf8mb4_unicode_ci`  
> 引擎: `InnoDB`

### 3.1 ER 关系图

```
┌──────────────────┐          ┌──────────────────┐
│    customers     │          │    vehicles      │
│  (客户信息表)     │ 1─────N  │   (车辆信息表)    │
├──────────────────┤          ├──────────────────┤
│ id (PK)          │◄─────────│ customer_id (FK) │
│ name             │          │ plate_number     │
│ gender (CHECK)   │          │ vin              │
│ phone (UNIQUE)   │          │ brand            │
│ id_card          │          │ model            │
│ address          │          │ color            │
│ email            │          │ year             │
│ remark           │          │ engine_number    │
│ is_deleted       │          │ register_date    │
│ create_time      │          │ is_deleted       │
│ update_time      │          │ create_time      │
└────────┬─────────┘          │ update_time      │
         │                    └────────┬─────────┘
         │                             │
         │          ┌──────────────────┘
         │          │
         ▼          ▼
┌──────────────────────────┐
│     beauty_orders        │
│    (美容订单表)           │
├──────────────────────────┤
│ id (PK)                  │
│ order_no (UNIQUE)        │
│ customer_id (FK)         │
│ vehicle_id (FK)          │
│ total_amount             │
│ discount_amount          │
│ payable_amount           │
│ status ─────────┐        │
│ appointment_time │        │
│ order_time       │       │  PENDING → IN_PROGRESS → COMPLETED → PAID
│ complete_time    │       │                     ↘ CANCELLED
│ remark           │       │
│ is_deleted       │       │
│ create_time      │       │
│ update_time      │       │
└────────┬─────────┘       │
         │                 │
         │ 1─────N         │ 1─────1
         ▼                 ▼
┌──────────────────┐  ┌──────────────────┐
│   order_items    │  │    payments      │
│  (订单明细表)     │  │  (支付记录表)     │
├──────────────────┤  ├──────────────────┤
│ id (PK)          │  │ id (PK)          │
│ order_id (FK)────┼──│ order_id (FK)    │
│ item_id (FK) ────┼┐ │ payment_no       │
│ quantity         ││ │ total_amount     │
│ unit_price       ││ │ discount_amount  │
│ subtotal         ││ │ paid_amount      │
│ create_time      ││ │ change_amount    │
└──────────────────┘│ │ payment_method───┼── CASH/WECHAT/ALIPAY/CARD/OTHER
                    │ │ pay_time         │
                    │ │ remark           │
                    │ │ create_time      │
                    │ └──────────────────┘
                    │
┌──────────────────┐│
│  beauty_items    ││
│ (美容项目表)      ││
├──────────────────┤│
│ id (PK)          │◄┘
│ item_name        │
│ item_code (UNIQUE)│
│ price            │
│ duration          │
│ description       │
│ status ───────────┼── 0-停用 / 1-启用
│ is_deleted        │
│ create_time       │
│ update_time       │
└───────────────────┘

┌──────────────────┐
│ backup_records   │
│ (备份记录表)       │
├──────────────────┤
│ id (PK)          │
│ backup_id (UNIQUE)│
│ backup_type ─────┼── FULL/INCREMENTAL/PARTIAL
│ file_name        │
│ file_path        │
│ file_size        │
│ status ──────────┼── PENDING/IN_PROGRESS/COMPLETED/FAILED/DELETED
│ tables           │
│ start_time       │
│ end_time         │
│ creator          │
│ description      │
│ md5_checksum     │
│ create_time      │
└──────────────────┘
```

### 3.2 数据库表详细定义

---

#### 3.2.1 customers（客户信息表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 客户ID（主键，自增） |
| `name` | VARCHAR(50) | NOT NULL | — | 客户姓名 |
| `gender` | VARCHAR(10) | NOT NULL, CHECK(男/女) | — | 性别（仅允许"男"或"女"） |
| `phone` | VARCHAR(11) | NOT NULL, UNIQUE | — | 手机号（11位，唯一） |
| `id_card` | VARCHAR(18) | — | NULL | 身份证号 |
| `address` | VARCHAR(200) | — | NULL | 地址 |
| `email` | VARCHAR(100) | — | NULL | 邮箱 |
| `remark` | VARCHAR(500) | — | NULL | 备注 |
| `is_deleted` | TINYINT | — | 0 | 逻辑删除：0-正常，1-已删除 |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | — | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |
| idx_customers_phone | phone | 普通索引 |

**约束**:
- `chk_gender`: `CHECK (gender IN ('男', '女'))` — 数据库层面强制性别二选一

---

#### 3.2.2 vehicles（车辆信息表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 车辆ID（主键，自增） |
| `customer_id` | BIGINT | NOT NULL, FK | — | 所属客户ID（外键 → customers.id） |
| `plate_number` | VARCHAR(20) | NOT NULL, UNIQUE | — | 车牌号（唯一） |
| `vin` | VARCHAR(17) | — | NULL | 车架号（VIN码） |
| `brand` | VARCHAR(50) | NOT NULL | — | 车辆品牌 |
| `model` | VARCHAR(50) | — | NULL | 车辆型号 |
| `color` | VARCHAR(20) | — | NULL | 车身颜色 |
| `year` | INT | — | NULL | 出厂年份 |
| `engine_number` | VARCHAR(30) | — | NULL | 发动机号 |
| `register_date` | DATE | — | NULL | 注册日期 |
| `is_deleted` | TINYINT | — | 0 | 逻辑删除 |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | — | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |
| idx_vehicles_customer_id | customer_id | 普通索引 |
| idx_vehicles_plate_number | plate_number | 普通索引 |

---

#### 3.2.3 beauty_items（美容项目表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 项目ID（主键，自增） |
| `item_name` | VARCHAR(50) | NOT NULL | — | 项目名称（如"全车打蜡"） |
| `item_code` | VARCHAR(20) | NOT NULL, UNIQUE | — | 项目编码（如"DM001"，唯一） |
| `price` | DECIMAL(10,2) | NOT NULL | — | 项目价格（元） |
| `duration` | INT | — | NULL | 服务时长（分钟） |
| `description` | VARCHAR(200) | — | NULL | 项目描述 |
| `status` | TINYINT | — | 1 | 状态：0-停用，1-启用 |
| `is_deleted` | TINYINT | — | 0 | 逻辑删除 |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | — | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |
| idx_beauty_items_code | item_code | 普通索引 |

---

#### 3.2.4 beauty_orders（美容订单表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 订单ID（主键，自增） |
| `order_no` | VARCHAR(32) | NOT NULL, UNIQUE | — | 订单号（格式: ORD + 年月日时分秒 + 4位随机码） |
| `customer_id` | BIGINT | NOT NULL, FK | — | 客户ID（外键 → customers.id） |
| `vehicle_id` | BIGINT | NOT NULL, FK | — | 车辆ID（外键 → vehicles.id） |
| `total_amount` | DECIMAL(10,2) | — | 0.00 | 总金额（项目单价 × 数量之和） |
| `discount_amount` | DECIMAL(10,2) | — | 0.00 | 折扣金额 |
| `payable_amount` | DECIMAL(10,2) | — | 0.00 | 应付金额（total - discount） |
| `status` | VARCHAR(20) | — | 'PENDING' | 订单状态（见下方状态枚举） |
| `appointment_time` | DATETIME | — | NULL | 预约时间 |
| `order_time` | DATETIME | — | CURRENT_TIMESTAMP | 下单时间 |
| `complete_time` | DATETIME | — | NULL | 完成时间 |
| `remark` | VARCHAR(500) | — | NULL | 订单备注 |
| `is_deleted` | TINYINT | — | 0 | 逻辑删除 |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | — | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**订单状态枚举**:

| 状态值 | 含义 | 说明 |
|--------|------|------|
| `PENDING` | 待处理 | 订单刚创建，等待施工 |
| `IN_PROGRESS` | 进行中 | 正在施工 |
| `COMPLETED` | 已完成 | 施工完成，等待收款 |
| `CANCELLED` | 已取消 | 订单取消 |
| `PAID` | 已支付 | 已完成收款 |

**状态流转规则**:

```
PENDING ──→ IN_PROGRESS ──→ COMPLETED ──→ PAID
    │                           │
    └─────────→ CANCELLED ←─────┘
```

> 只有 PENDING 和 IN_PROGRESS 状态的订单可以取消。  
> 只有 COMPLETED 状态的订单可以进行收款。

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |
| idx_orders_customer | (customer_id, order_time) | 复合索引 |
| idx_orders_order_no | order_no | 普通索引 |
| idx_orders_status | status | 普通索引 |

---

#### 3.2.5 order_items（订单明细表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 明细ID（主键，自增） |
| `order_id` | BIGINT | NOT NULL, FK | — | 订单ID（外键 → beauty_orders.id） |
| `item_id` | BIGINT | NOT NULL, FK | — | 美容项目ID（外键 → beauty_items.id） |
| `quantity` | INT | — | 1 | 服务数量 |
| `unit_price` | DECIMAL(10,2) | NOT NULL | — | 单价（快照自 beauty_items.price） |
| `subtotal` | DECIMAL(10,2) | NOT NULL | — | 小计（unit_price × quantity） |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |
| idx_order_items_order | order_id | 普通索引 |
| idx_order_items_item | item_id | 普通索引 |

---

#### 3.2.6 payments（支付记录表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 支付ID（主键，自增） |
| `order_id` | BIGINT | NOT NULL, FK | — | 订单ID（外键 → beauty_orders.id） |
| `payment_no` | VARCHAR(32) | NOT NULL, UNIQUE | — | 支付流水号（格式: PAY + 年月日时分秒 + 4位随机码） |
| `total_amount` | DECIMAL(10,2) | NOT NULL | — | 订单总金额 |
| `discount_amount` | DECIMAL(10,2) | — | 0.00 | 折扣金额 |
| `paid_amount` | DECIMAL(10,2) | NOT NULL | — | 实付金额 |
| `change_amount` | DECIMAL(10,2) | — | 0.00 | 找零金额 |
| `payment_method` | VARCHAR(20) | NOT NULL | — | 支付方式 |
| `pay_time` | DATETIME | — | CURRENT_TIMESTAMP | 支付时间 |
| `remark` | VARCHAR(200) | — | NULL | 备注 |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |

**支付方式枚举**:

| 方式 | 说明 |
|------|------|
| `CASH` | 现金 |
| `WECHAT` | 微信支付 |
| `ALIPAY` | 支付宝 |
| `CARD` | 银行卡刷卡 |
| `OTHER` | 其他方式 |

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |
| idx_payments_order | order_id | 普通索引 |

---

#### 3.2.7 backup_records（备份记录表）

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `id` | BIGINT | PK, AUTO_INCREMENT | — | 记录ID（主键，自增） |
| `backup_id` | VARCHAR(32) | NOT NULL, UNIQUE | — | 备份ID（格式: BK + 年月日时分秒） |
| `backup_type` | VARCHAR(20) | NOT NULL | — | 备份类型：FULL / INCREMENTAL / PARTIAL |
| `file_name` | VARCHAR(100) | NOT NULL | — | 备份文件名 |
| `file_path` | VARCHAR(255) | NOT NULL | — | 备份文件完整路径 |
| `file_size` | BIGINT | — | NULL | 文件大小（字节） |
| `status` | VARCHAR(20) | — | 'PENDING' | 状态：PENDING / IN_PROGRESS / COMPLETED / FAILED / DELETED |
| `tables` | TEXT | — | NULL | 备份的表列表（JSON 格式） |
| `start_time` | DATETIME | — | NULL | 备份开始时间 |
| `end_time` | DATETIME | — | NULL | 备份结束时间 |
| `creator` | VARCHAR(50) | — | NULL | 操作人 |
| `description` | VARCHAR(200) | — | NULL | 备份描述 |
| `md5_checksum` | VARCHAR(64) | — | NULL | 文件 MD5 校验值 |
| `create_time` | DATETIME | — | CURRENT_TIMESTAMP | 创建时间 |

**索引**:

| 索引名 | 字段 | 类型 |
|--------|------|------|
| PRIMARY | id | 主键 |

---

### 3.3 测试数据

系统初始化时自动插入以下测试数据：

**美容项目**:

| 项目名称 | 编码 | 价格 | 时长 | 描述 |
|---------|------|------|------|------|
| 全车打蜡 | DM001 | ¥298.00 | 60分钟 | 使用进口车蜡，全车身抛光打蜡 |
| 内饰清洗 | QN001 | ¥388.00 | 90分钟 | 深度清洗车内座椅、仪表盘等 |
| 玻璃镀膜 | BL001 | ¥588.00 | 120分钟 | 前挡风玻璃及侧窗镀膜 |
| 发动机舱清洗 | FD001 | ¥268.00 | 45分钟 | 发动机舱深度清洁除油 |
| 轮毂翻新 | LG001 | ¥358.00 | 90分钟 | 轮毂清洁抛光处理 |

**客户**:

| 姓名 | 性别 | 手机号 | 地址 |
|------|------|--------|------|
| 张三 | 男 | 13800138001 | 北京市朝阳区xxx路1号 |
| 李四 | 女 | 13900139002 | 上海市浦东新区xxx路2号 |
| 王五 | 男 | 13700137003 | 广州市天河区xxx路3号 |

---

## 4. 功能模块与业务流程

### 4.1 整体业务流程

```
                    ┌─────────────┐
                    │  系统工作台   │
                    │ (Dashboard)  │
                    └──────┬──────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                 ▼
   ┌──────────┐    ┌──────────────┐   ┌──────────┐
   │ 客户管理  │    │  美容项目管理  │   │ 统计分析  │
   │          │    │              │   │          │
   │ 新增客户  │    │ 维护项目列表   │   │ 月度统计  │
   │  ↓       │    │ 设置价格/状态  │   │ 年度统计  │
   │ 车辆管理  │    └──────┬───────┘   │ 收入统计  │
   └────┬─────┘           │           └──────────┘
        │                 │
        └────────┬────────┘
                 ▼
        ┌────────────────┐
        │   订单管理       │
        │                │
        │ 1. 选择客户     │
        │ 2. 选择车辆     │
        │ 3. 添加项目     │
        │ 4. 确认下单     │
        │ 5. 施工 → 完成  │
        │ 6. 收款结算     │
        └────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │  数据备份恢复    │
        │  mysqldump 备份 │
        │  mysql 恢复     │
        └────────────────┘
```

### 4.2 模块一：美容项目管理

**功能**: 维护门店提供的汽车美容服务项目目录。

**数据表**: `beauty_items`

**操作流程**:

```
管理员登录
  │
  ├── 查看项目列表（分页、搜索、状态筛选）
  │
  ├── 新增项目
  │     ├── 填写项目名称、编码（唯一）、价格、时长、描述
  │     ├── 校验编码唯一性
  │     └── 保存 → 项目状态默认启用
  │
  ├── 编辑项目
  │     ├── 修改名称、价格、时长、描述
  │     └── 保存更新
  │
  ├── 删除项目（逻辑删除，is_deleted = 1）
  │
  ├── 批量启用/停用
  │     └── 状态改为 0(停用) 或 1(启用)
  │
  └── 获取全部启用的项目列表（供订单创建时选择）
```

---

### 4.3 模块二：客户信息管理

**功能**: 维护到店客户的基本信息。

**数据表**: `customers`

**操作流程**:

```
管理员登录
  │
  ├── 查看客户列表（分页、姓名模糊搜索、手机号搜索、性别筛选）
  │
  ├── 新增客户
  │     ├── 填写姓名（必填）、性别（只能选"男"或"女"）、手机号（11位，必填，唯一）
  │     ├── 可选：身份证号、地址、邮箱、备注
  │     ├── 前端校验 + 后端校验
  │     │   ├── 性别必须是 "男" 或 "女"
  │     │   ├── 手机号格式校验
  │     │   └── 手机号唯一性检查（调用 /customers/check-phone）
  │     └── 保存
  │
  ├── 查看客户详情
  │     └── 返回客户信息 + 名下车辆列表
  │
  ├── 编辑客户
  │     ├── 修改基本信息
  │     └── 修改手机号时需再次校验唯一性（排除自身）
  │
  └── 删除客户（逻辑删除，is_deleted = 1）
```

**关键校验规则**:

| 字段 | 规则 | 实现层 |
|------|------|--------|
| gender | 只能是 "男" 或 "女" | 数据库 CHECK 约束 + Service 层双重校验 |
| phone | 11位数字，唯一 | 唯一索引 + Service 层查询校验 |
| name | 2-50 字符 | 前端表单校验 |

---

### 4.4 模块三：车辆管理

**功能**: 管理客户名下的车辆信息。一个客户可拥有多辆车。

**数据表**: `vehicles`（关联 `customers`）

**操作流程**:

```
管理员登录
  │
  ├── 查看车辆列表（分页、按客户ID/车牌号/品牌筛选）
  │     └── 列表显示客户姓名（关联查询）
  │
  ├── 新增车辆
  │     ├── 选择所属客户（必填）
  │     ├── 填写车牌号（必填，唯一）
  │     ├── 填写品牌（必填）
  │     ├── 可选：VIN码、型号、颜色、年份、发动机号、注册日期
  │     └── 保存
  │
  ├── 查看车辆详情
  │
  ├── 编辑车辆
  │     └── 修改车辆信息，车牌号修改需校验唯一性
  │
  └── 删除车辆（逻辑删除）
        └── 校验：有未完成订单的车辆禁止删除
```

---

### 4.5 模块四：美容登记与收费管理（核心模块）

**功能**: 创建美容订单、管理订单状态流转、收款结算。

**数据表**: `beauty_orders` + `order_items` + `payments`

#### 4.5.1 订单创建流程

```
┌─────────────────────────────────────────────────────────┐
│                    创建美容订单                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  步骤1: 选择客户                                         │
│    └── 从客户列表中选择（或先新增客户）                     │
│                                                         │
│  步骤2: 选择车辆                                         │
│    └── 从该客户名下的车辆中选择                            │
│    └── 校验：车辆必须属于所选客户                          │
│                                                         │
│  步骤3: 添加美容项目                                      │
│    └── 从启用状态的项目中选择                              │
│    └── 可添加多个项目，每个项目可设置数量                   │
│    └── 系统自动计算金额：                                  │
│        ┌──────────────────────────────────┐              │
│        │  遍历 order_items:                │              │
│        │    查询 beauty_items 获取价格      │              │
│        │    unit_price = 项目价格            │              │
│        │    subtotal = unit_price × quantity│             │
│        │    total_amount += subtotal        │              │
│        │                                    │              │
│        │  payable_amount = total_amount     │              │
│        │  discount_amount = 0 (初始)         │              │
│        └──────────────────────────────────┘              │
│                                                         │
│  步骤4: 设置预约时间（可选）                               │
│                                                         │
│  步骤5: 填写备注（可选）                                   │
│                                                         │
│  步骤6: 确认下单                                          │
│    └── 生成订单号: ORD + yyyyMMddHHmmss + 4位随机码       │
│    └── 订单状态 = PENDING                                 │
│    └── order_time = 当前时间                              │
│    └── @Transactional 事务保证原子性                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 4.5.2 订单状态流转

```
       创建订单
          │
          ▼
     ┌─────────┐
     │ PENDING │  待处理：订单刚创建，等待施工
     └────┬────┘
          │ 开始施工
          ▼
     ┌────────────┐
     │ IN_PROGRESS│  进行中：正在施工
     └────┬───────┘
          │
     ┌────┴────────────┐
     │ 施工完成         │ 取消订单
     ▼                 ▼
┌──────────┐    ┌───────────┐
│COMPLETED │    │ CANCELLED │  已取消
│  已完成   │    └───────────┘
└────┬─────┘
     │ 收款
     ▼
┌──────────┐
│   PAID   │  已支付：完成收款
└──────────┘
```

**状态流转规则**:
- `PENDING → IN_PROGRESS`: 开始施工
- `IN_PROGRESS → COMPLETED`: 施工完成（记录 complete_time）
- `COMPLETED → PAID`: 完成收款
- `PENDING / IN_PROGRESS → CANCELLED`: 取消订单
- 已完成(CANCELLED/PAID)的订单不可再修改状态

#### 4.5.3 收款流程

```
┌─────────────────────────────────────────────────────────┐
│                    订单收款                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  前提: 订单状态必须为 COMPLETED                            │
│                                                         │
│  步骤1: 选择支付方式                                      │
│    └── CASH / WECHAT / ALIPAY / CARD / OTHER            │
│                                                         │
│  步骤2: 输入折扣金额（可选，默认 0）                        │
│                                                         │
│  步骤3: 系统自动计算                                      │
│    ┌──────────────────────────────────┐                  │
│    │  payable = total - discount       │                  │
│    │  paid_amount = payable           │                  │
│    │  change_amount = 0               │                  │
│    └──────────────────────────────────┘                  │
│                                                         │
│  步骤4: 确认收款                                          │
│    ├── 生成支付流水号: PAY + yyyyMMddHHmmss + 4位随机码    │
│    ├── 写入 payments 表                                   │
│    ├── 更新订单: status = PAID                            │
│    │             discount_amount = 折扣金额               │
│    │             payable_amount = 应付金额                │
│    └── @Transactional 事务保证原子性                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 4.5.4 取消订单流程

```
PENDING / IN_PROGRESS 状态的订单
  │
  ├── 填写取消原因
  ├── 校验：只有 PENDING 或 IN_PROGRESS 可取消
  └── 更新: status = CANCELLED
```

#### 4.5.5 工作台（Dashboard）

调用 `/beauty-orders/statistics/overview` 获取当日数据概览：

- 今日订单数
- 待处理订单数
- 进行中订单数
- 已完成订单数
- 今日收入
- 本月收入

调用 `/beauty-orders/today/pending` 获取今日待处理订单列表。

---

### 4.6 模块五：统计分析

**功能**: 提供多维度经营数据统计报表。

**数据表**: 关联查询 `beauty_orders` + `order_items` + `beauty_items` + `customers`

**实现方式**: 使用 Spring `JdbcTemplate` 执行原生 SQL 聚合查询。（API 文档中设计了存储过程方案，实际代码中使用 Java 拼接 SQL 实现。）

#### 4.6.1 月度美容项目次数统计

**接口**: `GET /statistics/monthly-item-count?year=&month=`

**统计维度**:
- 每个美容项目的服务次数
- 总数量、总金额、平均金额
- 只统计 COMPLETED/PAID 状态的订单

#### 4.6.2 年度客户美容次数统计

**接口**: `GET /statistics/yearly-customer-count?year=`

**统计维度**:
- 每个客户的年度消费总次数、总金额
- 最近到店日期
- 客户等级评定：
  - ≥ ¥10,000 → VIP
  - ≥ ¥5,000 → 金卡
  - ≥ ¥2,000 → 银卡
  - 其他 → 普通

#### 4.6.3 月度收入统计

**接口**: `GET /statistics/monthly-revenue?year=&month=`

**统计维度**:
- 总订单数、完成数、取消数
- 毛收入、折扣总额、净收入
- 各支付方式收入分类（现金/微信/支付宝/刷卡）
- 日均收入趋势
- 各项目收入占比（饼图数据）

---

### 4.7 模块六：数据备份与恢复

**功能**: 保障数据安全，支持数据库备份、恢复、下载。

**数据表**: `backup_records`

**实现方式**: 通过 `Runtime.getRuntime().exec()` 调用系统命令行工具。

#### 4.7.1 创建备份

```
管理员操作
  │
  ├── 选择备份类型: FULL / INCREMENTAL / PARTIAL
  ├── 填写备份描述
  │
  └── 系统执行:
      ├── 生成 backup_id: BK + yyyyMMddHHmmss
      ├── 生成文件名: backup_yyyyMMdd_HHmmss.sql
      ├── 创建备份记录: status = IN_PROGRESS
      ├── 执行系统命令:
      │   mysqldump -u root -p[password] car_beauty > [file_path]
      ├── 命令完成后:
      │   成功 → status = COMPLETED, 记录文件大小
      │   失败 → status = FAILED, 记录错误信息
      └── 更新备份记录
```

#### 4.7.2 数据恢复

```
管理员操作
  │
  ├── 选择要恢复的备份记录
  ├── 确认密码
  │
  └── 系统执行:
      ├── 校验备份文件存在且状态为 COMPLETED
      ├── 执行系统命令:
      │   mysql -u root -p[password] car_beauty < [backup_file]
      └── 返回恢复结果
```

#### 4.7.3 其他操作

| 操作 | 说明 |
|------|------|
| 备份列表 | 分页查看所有备份记录 |
| 备份详情 | 查看备份基本信息、包含的表、文件大小 |
| 下载备份 | 返回备份文件路径供下载 |
| 删除备份 | 删除记录 + 物理删除备份文件 |

---

## 5. API 接口设计

### 5.1 通用规范

| 项目 | 说明 |
|------|------|
| 基础路径 | `http://localhost:8080/api/v1` |
| 响应格式 | `{ code: 200, message: "操作成功", data: {...}, timestamp: ... }` |
| 分页响应 | `{ records: [...], total: 100, page: 1, pageSize: 10 }` |
| 认证方式 | 当前版本未实现，预留 JWT Bearer Token 机制 |

### 5.2 接口汇总

#### 美容项目管理 — `/api/v1/beauty-items`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/beauty-items` | 分页查询（keyword, status 筛选） |
| GET | `/beauty-items/{id}` | 获取项目详情 |
| POST | `/beauty-items` | 新增项目 |
| PUT | `/beauty-items/{id}` | 修改项目 |
| DELETE | `/beauty-items/{id}` | 删除项目（逻辑删除） |
| PUT | `/beauty-items/status/batch` | 批量修改启用/停用状态 |
| GET | `/beauty-items/all` | 获取全部启用项目（供订单选择） |

#### 客户管理 — `/api/v1/customers`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/customers` | 分页查询（name, phone, gender 筛选） |
| GET | `/customers/{id}` | 获取客户详情（含车辆列表） |
| POST | `/customers` | 新增客户（校验手机号唯一、性别值） |
| PUT | `/customers/{id}` | 修改客户信息 |
| DELETE | `/customers/{id}` | 删除客户（逻辑删除） |
| GET | `/customers/check-phone` | 检查手机号是否存在 |

#### 车辆管理 — `/api/v1/vehicles`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/vehicles` | 分页查询（customerId, plateNumber, brand 筛选） |
| GET | `/vehicles/{id}` | 获取车辆详情 |
| POST | `/vehicles` | 新增车辆 |
| PUT | `/vehicles/{id}` | 修改车辆信息 |
| DELETE | `/vehicles/{id}` | 删除车辆（逻辑删除） |

#### 订单管理 — `/api/v1/beauty-orders`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/beauty-orders` | 分页查询（orderNo, customerId, status 筛选） |
| GET | `/beauty-orders/{orderId}` | 获取订单详情（含明细 + 支付信息） |
| POST | `/beauty-orders` | 创建订单（自动计算金额、生成订单号） |
| PUT | `/beauty-orders/{orderId}/status` | 更新订单状态 |
| POST | `/beauty-orders/{orderId}/payment` | 订单收款 |
| PUT | `/beauty-orders/{orderId}/cancel` | 取消订单 |
| GET | `/beauty-orders/today/pending` | 今日待处理订单 |
| GET | `/beauty-orders/statistics/overview` | 工作台统计概览 |

#### 统计分析 — `/api/v1/statistics`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/statistics/monthly-item-count` | 月度美容项目次数统计 |
| GET | `/statistics/yearly-customer-count` | 年度客户美容次数统计 |
| GET | `/statistics/monthly-revenue` | 月度收入统计 |

#### 数据备份 — `/api/v1/backup`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/backup/create` | 创建备份 |
| GET | `/backup/list` | 备份列表 |
| GET | `/backup/{backupId}` | 备份详情 |
| GET | `/backup/{backupId}/download` | 下载备份文件 |
| POST | `/backup/{backupId}/restore` | 恢复数据 |
| GET | `/backup/restore/{restoreId}/status` | 恢复进度查询 |
| DELETE | `/backup/{backupId}` | 删除备份 |

---

## 6. 前端页面结构

### 6.1 路由配置

| 路径 | 视图组件 | 页面名称 | 说明 |
|------|---------|---------|------|
| `/` | — | — | 重定向到 /dashboard |
| `/dashboard` | Dashboard.vue | 工作台 | 首页统计卡片 + 待处理订单 |
| `/beauty-items` | BeautyItemManage.vue | 美容项目管理 | 项目 CRUD 表格 |
| `/customers` | CustomerManage.vue | 客户管理 | 客户 CRUD 表格 |
| `/vehicles` | VehicleManage.vue | 车辆管理 | 车辆 CRUD 表格 |
| `/orders` | OrderManage.vue | 订单管理 | 订单创建 + 列表 + 收款 |
| `/statistics` | Statistics.vue | 统计分析 | 三个统计 Tab 页 |
| `/backup` | BackupManage.vue | 数据备份 | 备份创建 + 列表 + 恢复 |

### 6.2 布局结构

```
┌──────────────────────────────────────────────┐
│  Logo   汽车美容管理系统                       │ ← 顶栏
├──────────┬───────────────────────────────────┤
│          │                                   │
│  工作台   │                                   │
│  项目管理  │         <router-view />           │
│  客户管理  │         页面内容区域               │
│  车辆管理  │                                   │
│  订单管理  │                                   │
│  统计分析  │                                   │
│  数据备份  │                                   │
│          │                                   │
├──────────┴───────────────────────────────────┤
│ 侧边栏(220px)        内容区域(自适应)           │
└──────────────────────────────────────────────┘
```

- **侧边栏**: Element Plus `el-menu` 深色主题，`router` 模式与 Vue Router 联动
- **顶栏**: 系统名称 + 面包屑导航
- **内容区**: 各页面通过 `<router-view />` 渲染

### 6.3 API 请求封装

前端 API 层采用模块化设计：

```
src/api/
├── request.js       # Axios 实例 + 请求/响应拦截器
│   ├── baseURL: '/api/v1'
│   ├── 响应拦截: 统一错误提示、code 非 200 时弹窗报错
│   └── 导出 get / post / put / del 方法
│
├── beautyItem.js    # 美容项目相关 API
├── customer.js      # 客户相关 API
├── vehicle.js       # 车辆相关 API
├── order.js         # 订单相关 API
├── statistics.js    # 统计相关 API
└── backup.js        # 备份相关 API
```

---

## 7. 部署与运行

### 7.1 环境要求

| 组件 | 版本要求 |
|------|---------|
| JDK | 1.8+ |
| Maven | 3.6+ |
| Node.js | 16+ |
| MySQL | 8.0+ |
| mysqldump | MySQL 客户端工具（备份功能需要） |

### 7.2 后端配置

**application.yml 关键配置**:

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_beauty?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.carmanage.entity
  configuration:
    map-underscore-to-camel-case: true   # 下划线转驼峰
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # SQL 日志

car-manage:
  backup:
    path: ./backups   # 备份文件存放路径
```

### 7.3 前端配置

**vite.config.js 关键配置**:

```javascript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

### 7.4 启动步骤

```bash
# 1. 初始化数据库
mysql -u root -p < backend/src/main/resources/db/init.sql

# 2. 启动后端
cd backend
mvn spring-boot:run
# 后端运行在 http://localhost:8080

# 3. 启动前端
cd frontend
npm install
npm run dev
# 前端运行在 http://localhost:5173
```

### 7.5 关键技术设计说明

| 设计点 | 实现方式 | 说明 |
|--------|---------|------|
| 逻辑删除 | MyBatis-Plus `@TableLogic` | 所有业务表使用 `is_deleted` 字段，删除操作统一为 UPDATE |
| 自动填充 | `MyMetaObjectHandler` | `createTime` 和 `updateTime` 自动填充，无需手动设置 |
| 分页查询 | MyBatis-Plus `PaginationInnerInterceptor` | 所有列表接口支持分页 |
| 响应封装 | `Result<T>` + `PageResult<T>` | 统一的 JSON 响应结构 |
| 异常处理 | `GlobalExceptionHandler` | `@RestControllerAdvice` 全局捕获异常，统一返回错误格式 |
| CORS | `CorsConfig` | 允许前端跨域请求 |
| 事务管理 | `@Transactional` | 订单创建、收款等关键操作使用事务保证数据一致性 |
| 订单号生成 | 时间戳 + 随机码 | `ORD` + `yyyyMMddHHmmss` + 4位随机数 |
| 支付流水号 | 时间戳 + 随机码 | `PAY` + `yyyyMMddHHmmss` + 4位随机数 |
| 备份实现 | `Runtime.exec()` | 调用系统命令 `mysqldump` / `mysql` |

---

> **文档结束** — 本文档描述了汽车美容店管理系统的完整技术架构、数据库设计、业务流程和部署说明。如有疑问请参考 `API接口文档.md` 获取更详细的接口定义。
